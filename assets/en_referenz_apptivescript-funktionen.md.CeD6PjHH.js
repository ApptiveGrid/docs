import{_ as t,o as d,c as o,a3 as a}from"./chunks/framework.B5WAEDB7.js";const u=JSON.parse('{"title":"ApptiveScript Functions","description":"All functions of the ApptiveScript expression language, organized by value type.","frontmatter":{"title":"ApptiveScript Functions","description":"All functions of the ApptiveScript expression language, organized by value type."},"headers":[],"relativePath":"en/referenz/apptivescript-funktionen.md","filePath":"en/referenz/apptivescript-funktionen.md","lastUpdated":1788784095000}'),s={name:"en/referenz/apptivescript-funktionen.md"};function n(c,e,i,r,l,p){return d(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="apptivescript-functions" tabindex="-1">ApptiveScript Functions <a class="header-anchor" href="#apptivescript-functions" aria-label="Permalink to &quot;ApptiveScript Functions&quot;">​</a></h1><p>The complete list of functions available in an expression — in formula columns, in the conditional visibility of blocks, in Flow conditions, and in filters.</p><p>This page is meant for reference. If you&#39;re writing an expression for the first time, start at <a href="/en/apptivescript/was-ist-apptivescript">What is ApptiveScript</a>.</p><h2 id="how-to-read-the-tables" tabindex="-1">How to read the tables <a class="header-anchor" href="#how-to-read-the-tables" aria-label="Permalink to &quot;How to read the tables&quot;">​</a></h2><p>An expression is always <strong>value, dot, function</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;preis&#39;).multiplyBy(1.19)</span></span></code></pre></div><p>To the left of the dot is a value; to the right, a function from the table for <strong>its type</strong>. The result again has a type — and you can apply another function to it:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;nachname&#39;).concat(fieldValue(&#39;vorname&#39;), &#39;, &#39;).isNotEmpty()</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Column</th><th>Meaning</th></tr></thead><tbody><tr><td><strong>Function</strong></td><td>The name, exactly as it&#39;s written. Entries with a leading dot and no parentheses — such as <code>.email</code> — are properties and take no arguments.</td></tr><tr><td><strong>Arguments</strong></td><td>What goes inside the parentheses, in this order: <code>name: Type</code>.</td></tr><tr><td><strong>Result</strong></td><td>The type of the result. It determines which table applies for the next dot. <code>Any</code> means: a value whose type is only known when it&#39;s evaluated — for example, a value from JSON data.</td></tr></tbody></table><p>Function names are in <strong>English</strong> and stay that way even in the German interface. Capitalization is mandatory.</p><div class="tip custom-block"><p class="custom-block-title">The type is shown in the editor</p><p>The <em>Edit an expression</em> dialog shows the determined type above the sample result — <em>Type: String</em>, <em>Type: Number</em>, and so on. That&#39;s how you find the right table for the next dot. The <strong>Search</strong> tab in the editor only lists functions for the currently determined type anyway.</p></div><h2 id="with-or-without-curly-braces" tabindex="-1">With or without curly braces <a class="header-anchor" href="#with-or-without-curly-braces" aria-label="Permalink to &quot;With or without curly braces&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Where you write</th><th>Notation</th></tr></thead><tbody><tr><td>Formula editor, visibility, advanced Flow logic</td><td>just the expression: <code>fieldValue(&#39;preis&#39;).multiplyBy(1.19)</code></td></tr><tr><td>A regular text field in a Flow step</td><td>inside braces: <code>Your invoice for {{ fieldValue(&#39;betrag&#39;) }} Euro</code></td></tr></tbody></table><p>An expression without braces keeps its type. As soon as you embed it in text, the result becomes text — and can no longer be used in further calculations.</p><h2 id="getting-values-into-an-expression" tabindex="-1">Getting values into an expression <a class="header-anchor" href="#getting-values-into-an-expression" aria-label="Permalink to &quot;Getting values into an expression&quot;">​</a></h2><p>What an expression starts with depends on where it&#39;s used:</p><table tabindex="0"><thead><tr><th>Starting point</th><th>Meaning</th><th>Used in</th></tr></thead><tbody><tr><td><code>fieldValue(&#39;&lt;field-id&gt;&#39;)</code></td><td>a field of the current entry</td><td><a href="/en/grids/berechnete-felder#formula">Formula columns</a>, filters</td></tr><tr><td><code>formValues.get(&#39;&lt;key&gt;&#39;)</code></td><td>an input value from the page</td><td><a href="/en/seiten/sichtbarkeit">conditional visibility</a></td></tr><tr><td><code>step(&#39;&lt;step-id&gt;&#39;).output</code></td><td>the result of an earlier Flow step</td><td><a href="/en/flows/daten-zwischen-schritten">Flows</a></td></tr><tr><td><code>today()</code></td><td>today&#39;s date</td><td>everywhere</td></tr><tr><td><code>now()</code></td><td>the current point in time</td><td>everywhere</td></tr><tr><td><code>true</code>, <code>false</code></td><td>fixed boolean values</td><td>everywhere</td></tr></tbody></table><p>Nobody types field IDs and step IDs by hand — the editor inserts them via the picker. The <strong>key</strong> of an input block, on the other hand, is chosen by you; it&#39;s shown under <em>Manage keys</em>.</p><div class="warning custom-block"><p class="custom-block-title">IDs are not names</p><p><code>fieldValue(&#39;…&#39;)</code> refers to the <strong>ID</strong> of a column, not its name. That&#39;s intentional: a formula survives renaming the column. But it also means an expression can&#39;t be checked just by reading the field names.</p></div><h2 id="empty-values" tabindex="-1">Empty values <a class="header-anchor" href="#empty-values" aria-label="Permalink to &quot;Empty values&quot;">​</a></h2><p>A field that isn&#39;t filled in doesn&#39;t produce an error — it produces an empty value. Two functions handle that:</p><table tabindex="0"><thead><tr><th>Function</th><th>Effect</th></tr></thead><tbody><tr><td><code>ifUndefined(&lt;replacement&gt;)</code></td><td>inserts a replacement value if the value is missing</td></tr><tr><td><code>isEmpty()</code> / <code>isNotEmpty()</code></td><td>checks whether a value is missing</td></tr></tbody></table><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;spitzname&#39;).ifUndefined(&#39;unbekannt&#39;)</span></span></code></pre></div><p><code>ifUndefined</code> exists on every type; <code>isEmpty()</code> and <code>isNotEmpty()</code> on almost every type — not on <em>Number, general</em>. The tables below spell out exactly where.</p><h2 id="getting-a-value-out-of-a-list" tabindex="-1">Getting a value out of a list <a class="header-anchor" href="#getting-a-value-out-of-a-list" aria-label="Permalink to &quot;Getting a value out of a list&quot;">​</a></h2><p>Some functions return a <strong>list</strong> instead of a single value — <code>match()</code>, for example, which collects all matches of a search pattern. Use <code>at()</code> to reach a single entry:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;a,b,c&#39;.match(&#39;[a-z]&#39;).at(1)</span></span></code></pre></div><p>This returns <code>a</code> — <strong>the first entry is number 1</strong>, not 0. <code>size()</code> tells you how many there are.</p><div class="tip custom-block"><p class="custom-block-title">A list is not a displayable value</p><p>A <a href="/en/grids/berechnete-felder#formula">formula column</a> must end on a single value. If the expression ends on a list, the column can&#39;t display it — append <code>at()</code>.</p></div><p><em>Generated from ApptiveScript <strong>0.4.13</strong> using <code>npm run apptivescript:reference</code>.</em></p><h2 id="available-on-every-type" tabindex="-1">Available on every type <a class="header-anchor" href="#available-on-every-type" aria-label="Permalink to &quot;Available on every type&quot;">​</a></h2><p>These functions exist regardless of what&#39;s to the left of the dot. They&#39;re not repeated in the tables for the individual types.</p><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>ifUndefined(…)</code></td><td>default: Any</td><td><code>Any</code></td></tr><tr><td><code>isEqualTo(…)</code></td><td>other: same type</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEqualTo(…)</code></td><td>other: same type</td><td><code>Boolean</code></td></tr></tbody></table><h2 id="text" tabindex="-1">Text <a class="header-anchor" href="#text" aria-label="Permalink to &quot;Text&quot;">​</a></h2><h3 id="text-string" tabindex="-1">Text (String) <a class="header-anchor" href="#text-string" aria-label="Permalink to &quot;Text (String)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asDateAndTime()</code></td><td>—</td><td><code>DateAndTime</code></td></tr><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringContent()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringPretty()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asNumber()</code></td><td>—</td><td><code>Number</code></td></tr><tr><td><code>beginsWith(…)</code></td><td>prefix: String</td><td><code>Boolean</code></td></tr><tr><td><code>concat(…)</code></td><td>string: String, separator: String</td><td><code>String</code></td></tr><tr><td><code>contains(…)</code></td><td>substring: String</td><td><code>Boolean</code></td></tr><tr><td><code>endsWith(…)</code></td><td>postfix: String</td><td><code>Boolean</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>match(…)</code></td><td>pattern: String</td><td><code>Collection</code></td></tr><tr><td><code>parseJSON()</code></td><td>—</td><td><code>KeyValueObject</code></td></tr><tr><td><code>sha256()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>substring(…)</code></td><td>from: Number, to: Number</td><td><code>String</code></td></tr></tbody></table><h3 id="single-select-enum" tabindex="-1">Single Select (Enum) <a class="header-anchor" href="#single-select-enum" aria-label="Permalink to &quot;Single Select (Enum)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asDateAndTime()</code></td><td>—</td><td><code>DateAndTime</code></td></tr><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringContent()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringPretty()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asNumber()</code></td><td>—</td><td><code>Number</code></td></tr><tr><td><code>beginsWith(…)</code></td><td>prefix: String</td><td><code>Boolean</code></td></tr><tr><td><code>concat(…)</code></td><td>string: String, separator: String</td><td><code>String</code></td></tr><tr><td><code>contains(…)</code></td><td>substring: String</td><td><code>Boolean</code></td></tr><tr><td><code>endsWith(…)</code></td><td>postfix: String</td><td><code>Boolean</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>match(…)</code></td><td>pattern: String</td><td><code>Collection</code></td></tr><tr><td><code>parseJSON()</code></td><td>—</td><td><code>KeyValueObject</code></td></tr><tr><td><code>substring(…)</code></td><td>from: Number, to: Number</td><td><code>String</code></td></tr></tbody></table><h3 id="e-mail-string" tabindex="-1">E-Mail (String) <a class="header-anchor" href="#e-mail-string" aria-label="Permalink to &quot;E-Mail (String)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringContent()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringPretty()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>beginsWith(…)</code></td><td>prefix: String</td><td><code>Boolean</code></td></tr><tr><td><code>concat(…)</code></td><td>string: String, separator: String</td><td><code>String</code></td></tr><tr><td><code>contains(…)</code></td><td>substring: String</td><td><code>Boolean</code></td></tr><tr><td><code>endsWith(…)</code></td><td>postfix: String</td><td><code>Boolean</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>match(…)</code></td><td>pattern: String</td><td><code>Collection</code></td></tr><tr><td><code>parseJSON()</code></td><td>—</td><td><code>KeyValueObject</code></td></tr><tr><td><code>substring(…)</code></td><td>from: Number, to: Number</td><td><code>String</code></td></tr></tbody></table><h2 id="numbers" tabindex="-1">Numbers <a class="header-anchor" href="#numbers" aria-label="Permalink to &quot;Numbers&quot;">​</a></h2><h3 id="number-general-number" tabindex="-1">Number, general (Number) <a class="header-anchor" href="#number-general-number" aria-label="Permalink to &quot;Number, general (Number)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>divideBy(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>greaterThan(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr><tr><td><code>greaterThanOrEqual(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr><tr><td><code>minus(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>multiplyBy(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>plus(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>round(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>roundDown(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>roundUp(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>smallerThan(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr><tr><td><code>smallerThanOrEqual(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr></tbody></table><h3 id="number-integer" tabindex="-1">Number (Integer) <a class="header-anchor" href="#number-integer" aria-label="Permalink to &quot;Number (Integer)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asDateAndTime()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringContent()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringPretty()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>divideBy(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>greaterThan(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr><tr><td><code>greaterThanOrEqual(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>minus(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>multiplyBy(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>plus(…)</code></td><td>number: Number</td><td><code>Number</code></td></tr><tr><td><code>round(…)</code></td><td>number: Number</td><td><code>Decimal</code></td></tr><tr><td><code>roundDown(…)</code></td><td>number: Number</td><td><code>Decimal</code></td></tr><tr><td><code>roundUp(…)</code></td><td>number: Number</td><td><code>Decimal</code></td></tr><tr><td><code>second()</code></td><td>—</td><td><code>Duration</code></td></tr><tr><td><code>seconds()</code></td><td>—</td><td><code>Duration</code></td></tr><tr><td><code>smallerThan(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr><tr><td><code>smallerThanOrEqual(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr></tbody></table><h3 id="decimal-and-currency-decimal" tabindex="-1">Decimal and Currency (Decimal) <a class="header-anchor" href="#decimal-and-currency-decimal" aria-label="Permalink to &quot;Decimal and Currency (Decimal)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asDateAndTime()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringContent()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringPretty()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>divideBy(…)</code></td><td>number: Number</td><td><code>Decimal</code></td></tr><tr><td><code>greaterThan(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr><tr><td><code>greaterThanOrEqual(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>minus(…)</code></td><td>number: Number</td><td><code>Decimal</code></td></tr><tr><td><code>multiplyBy(…)</code></td><td>number: Number</td><td><code>Decimal</code></td></tr><tr><td><code>plus(…)</code></td><td>number: Number</td><td><code>Decimal</code></td></tr><tr><td><code>round(…)</code></td><td>number: Number</td><td><code>Decimal</code></td></tr><tr><td><code>roundDown(…)</code></td><td>number: Number</td><td><code>Decimal</code></td></tr><tr><td><code>roundUp(…)</code></td><td>number: Number</td><td><code>Decimal</code></td></tr><tr><td><code>smallerThan(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr><tr><td><code>smallerThanOrEqual(…)</code></td><td>number: Number</td><td><code>Boolean</code></td></tr></tbody></table><h2 id="date-and-time" tabindex="-1">Date and time <a class="header-anchor" href="#date-and-time" aria-label="Permalink to &quot;Date and time&quot;">​</a></h2><h3 id="date-date" tabindex="-1">Date (Date) <a class="header-anchor" href="#date-date" aria-label="Permalink to &quot;Date (Date)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>add(…)</code></td><td>amount: Number, unit: String</td><td><code>DateAndTime</code></td></tr><tr><td><code>addDays(…)</code></td><td>numberOfDays: Number</td><td><code>Date</code></td></tr><tr><td><code>after(…)</code></td><td>date: Date</td><td><code>Boolean</code></td></tr><tr><td><code>before(…)</code></td><td>date: Date</td><td><code>Boolean</code></td></tr><tr><td><code>difference(…)</code></td><td>date: Date</td><td><code>Duration</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isoString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>minus(…)</code></td><td>date: Date</td><td><code>Duration</code></td></tr><tr><td><code>plus(…)</code></td><td>duration: Duration</td><td><code>Duration</code></td></tr><tr><td><code>subtract(…)</code></td><td>amount: Number, unit: String</td><td><code>DateAndTime</code></td></tr><tr><td><code>subtractDays(…)</code></td><td>numberofDays: Number</td><td><code>Date</code></td></tr></tbody></table><h3 id="date-and-time-dateandtime" tabindex="-1">Date and time (DateAndTime) <a class="header-anchor" href="#date-and-time-dateandtime" aria-label="Permalink to &quot;Date and time (DateAndTime)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>add(…)</code></td><td>amount: Number, unit: String</td><td><code>DateAndTime</code></td></tr><tr><td><code>after(…)</code></td><td>dateAndTime: DateAndTime</td><td><code>Boolean</code></td></tr><tr><td><code>before(…)</code></td><td>dateAndTime: DateAndTime</td><td><code>Boolean</code></td></tr><tr><td><code>date()</code></td><td>—</td><td><code>Date</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isoString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>minus(…)</code></td><td>dateAndTime: DateAndTime</td><td><code>Duration</code></td></tr><tr><td><code>plus(…)</code></td><td>dateAndTime: DateAndTime</td><td><code>Duration</code></td></tr><tr><td><code>subtract(…)</code></td><td>amount: Number, unit: String</td><td><code>DateAndTime</code></td></tr><tr><td><code>toTimezoneString(…)</code></td><td>timezone: String</td><td><code>String</code></td></tr></tbody></table><h3 id="duration-duration" tabindex="-1">Duration (Duration) <a class="header-anchor" href="#duration-duration" aria-label="Permalink to &quot;Duration (Duration)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asDays()</code></td><td>—</td><td><code>Decimal</code></td></tr><tr><td><code>asHours()</code></td><td>—</td><td><code>Decimal</code></td></tr><tr><td><code>asMinutes()</code></td><td>—</td><td><code>Decimal</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr></tbody></table><h2 id="booleans" tabindex="-1">Booleans <a class="header-anchor" href="#booleans" aria-label="Permalink to &quot;Booleans&quot;">​</a></h2><h3 id="checkmark-and-decision-boolean" tabindex="-1">Checkmark and Decision (Boolean) <a class="header-anchor" href="#checkmark-and-decision-boolean" aria-label="Permalink to &quot;Checkmark and Decision (Boolean)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringContent()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringPretty()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>if(…)</code></td><td>if: Any, else: Any</td><td><code>Any</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isFalse()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isTrue()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>logicalAnd(…)</code></td><td>other: Boolean</td><td><code>Boolean</code></td></tr><tr><td><code>logicalOr(…)</code></td><td>other: Boolean</td><td><code>Boolean</code></td></tr></tbody></table><h2 id="lists" tabindex="-1">Lists <a class="header-anchor" href="#lists" aria-label="Permalink to &quot;Lists&quot;">​</a></h2><h3 id="list-general-collection" tabindex="-1">List, general (Collection) <a class="header-anchor" href="#list-general-collection" aria-label="Permalink to &quot;List, general (Collection)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>add(…)</code></td><td>element: Any</td><td><code>Collection</code></td></tr><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringContent()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringPretty()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asList()</code></td><td>—</td><td><code>Collection</code></td></tr><tr><td><code>asString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>at(…)</code></td><td>index: Integer</td><td><code>Any</code></td></tr><tr><td><code>includesAllOf(…)</code></td><td>entities: Collection</td><td><code>Boolean</code></td></tr><tr><td><code>includesAnyOf(…)</code></td><td>entities: Collection</td><td><code>Boolean</code></td></tr><tr><td><code>includesNoneOf(…)</code></td><td>entities: Collection</td><td><code>Boolean</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>size()</code></td><td>—</td><td><code>Integer</code></td></tr></tbody></table><h3 id="multiple-select-enumcollection" tabindex="-1">Multiple Select (EnumCollection) <a class="header-anchor" href="#multiple-select-enumcollection" aria-label="Permalink to &quot;Multiple Select (EnumCollection)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringContent()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringPretty()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>at(…)</code></td><td>index: Integer</td><td><code>Any</code></td></tr><tr><td><code>includes(…)</code></td><td>entity: Boolean</td><td><code>EnumCollection</code></td></tr><tr><td><code>includesAll(…)</code></td><td>entities: Collection</td><td><code>EnumCollection</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>size()</code></td><td>—</td><td><code>Integer</code></td></tr></tbody></table><h3 id="link-multiple-records-entitycollection" tabindex="-1">Link multiple records (EntityCollection) <a class="header-anchor" href="#link-multiple-records-entitycollection" aria-label="Permalink to &quot;Link multiple records (EntityCollection)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>add(…)</code></td><td>element: String</td><td><code>EntityCollection</code></td></tr><tr><td><code>addEntity(…)</code></td><td>element: String</td><td><code>EntityCollection</code></td></tr><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringContent()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringPretty()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>at(…)</code></td><td>index: Integer</td><td><code>Any</code></td></tr><tr><td><code>includes(…)</code></td><td>entity: Boolean</td><td><code>EntityCollection</code></td></tr><tr><td><code>includesAll(…)</code></td><td>entities: Collection</td><td><code>EntityCollection</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>size()</code></td><td>—</td><td><code>Integer</code></td></tr></tbody></table><h2 id="composite-values" tabindex="-1">Composite values <a class="header-anchor" href="#composite-values" aria-label="Permalink to &quot;Composite values&quot;">​</a></h2><h3 id="json-object-keyvalueobject" tabindex="-1">JSON object (KeyValueObject) <a class="header-anchor" href="#json-object-keyvalueobject" aria-label="Permalink to &quot;JSON object (KeyValueObject)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringContent()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asJSONStringPretty()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>get(…)</code></td><td>key: String</td><td><code>Any</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr></tbody></table><h3 id="files-attachment" tabindex="-1">Files (Attachment) <a class="header-anchor" href="#files-attachment" aria-label="Permalink to &quot;Files (Attachment)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>largeThumbnail()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>smallThumbnail()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>url()</code></td><td>—</td><td><code>String</code></td></tr></tbody></table><h3 id="geolocation-geocoordinate" tabindex="-1">Geolocation (GeoCoordinate) <a class="header-anchor" href="#geolocation-geocoordinate" aria-label="Permalink to &quot;Geolocation (GeoCoordinate)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>asJSONString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>asString()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>lat()</code></td><td>—</td><td><code>Number</code></td></tr><tr><td><code>latitude()</code></td><td>—</td><td><code>Number</code></td></tr><tr><td><code>lon()</code></td><td>—</td><td><code>Number</code></td></tr><tr><td><code>longitude()</code></td><td>—</td><td><code>Number</code></td></tr></tbody></table><h3 id="user-user" tabindex="-1">User (User) <a class="header-anchor" href="#user-user" aria-label="Permalink to &quot;User (User)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>.email</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>.firstName</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>.id</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>.lastName</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr></tbody></table><h3 id="created-by-createdbyvalue" tabindex="-1">Created by (CreatedByValue) <a class="header-anchor" href="#created-by-createdbyvalue" aria-label="Permalink to &quot;Created by (CreatedByValue)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Function</th><th>Arguments</th><th>Result</th></tr></thead><tbody><tr><td><code>.email</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>.id</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>.type</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>displayValue()</code></td><td>—</td><td><code>String</code></td></tr><tr><td><code>isEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr><tr><td><code>isNotEmpty()</code></td><td>—</td><td><code>Boolean</code></td></tr></tbody></table><h2 id="examples" tabindex="-1">Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;Examples&quot;">​</a></h2><p>A selection of frequently used functions with what they do and an example. The full signature is in the tables above — this section is about what the function does.</p><p><strong><code>add(amount: Number, unit: String)</code></strong> → <code>DateAndTime</code></p><p>Adds an amount in the given unit to a date. Confirmed to work: <code>&#39;day&#39;</code>, <code>&#39;week&#39;</code>, <code>&#39;year&#39;</code>, <code>&#39;hour&#39;</code> and <code>&#39;minute&#39;</code> (singular) — <code>&#39;month&#39;</code>/<code>&#39;months&#39;</code> do not; there is no substitute for months, for days use <code>addDays()</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;start&#39;).add(1, &#39;week&#39;)</span></span></code></pre></div><p>Result: the date one week later</p><p><strong><code>addDays(numberOfDays: Number)</code></strong> → <code>Date</code></p><p>Adds days to a date.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;liefertermin&#39;).addDays(7)</span></span></code></pre></div><p>Result: the date one week later</p><p><strong><code>addEntity(element: String)</code></strong> → <code>EntityCollection</code></p><p>Links one more record by its id.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;verknuepft&#39;).addEntity(fieldValue(&#39;neuerEintrag&#39;))</span></span></code></pre></div><p>Result: the list with the additional record</p><p><strong><code>after(date: Date)</code></strong> → <code>Boolean</code></p><p>Checks whether the date lies after the argument.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;start&#39;).after(fieldValue(&#39;ende&#39;))</span></span></code></pre></div><p>Result: <code>true</code> when <em>start</em> lies after <em>ende</em></p><p><strong><code>asDateAndTime()</code></strong> → <code>DateAndTime</code></p><p>Reads text in ISO format as a date with time.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;2026-08-18T10:00:00Z&#39;.asDateAndTime()</span></span></code></pre></div><p>Result: the same point in time as a <code>DateAndTime</code></p><p><strong><code>asDays()</code></strong> → <code>Decimal</code></p><p>The time span as a number of days.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;ende&#39;).difference(fieldValue(&#39;start&#39;)).asDays()</span></span></code></pre></div><p>Result: the time span in days, e.g. <code>2.5</code></p><p><strong><code>asHours()</code></strong> → <code>Decimal</code></p><p>The time span as a number of hours.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;ende&#39;).difference(fieldValue(&#39;start&#39;)).asHours()</span></span></code></pre></div><p>Result: the time span in hours</p><p><strong><code>asJSONStringContent()</code></strong> → <code>String</code></p><p>The JSON representation of the value, without the outer quotes for text.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;abc&#39;.asJSONStringContent()</span></span></code></pre></div><p>Result: <code>&#39;abc&#39;</code></p><p><strong><code>asJSONStringPretty()</code></strong> → <code>String</code></p><p>The JSON representation of the value, with quotes for text.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;abc&#39;.asJSONStringPretty()</span></span></code></pre></div><p>Result: <code>&quot;abc&quot;</code></p><p><strong><code>asList()</code></strong> → <code>Collection</code></p><p>Returns an empty list instead of nothing when no value is present — the counterpart of <code>ifUndefined()</code> for lists.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;verknuepft&#39;).asList()</span></span></code></pre></div><p>Result: the list, or an empty list when the field is empty</p><p><strong><code>asMinutes()</code></strong> → <code>Decimal</code></p><p>The time span as a number of minutes.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;ende&#39;).difference(fieldValue(&#39;start&#39;)).asMinutes()</span></span></code></pre></div><p>Result: the time span in minutes</p><p><strong><code>asNumber()</code></strong> → <code>Number</code></p><p>Converts text into a number.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;42&#39;.asNumber()</span></span></code></pre></div><p>Result: <code>42</code></p><p><strong><code>asString()</code></strong> → <code>String</code></p><p>Converts the number into text.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;menge&#39;).asString()</span></span></code></pre></div><p>Result: the number as text, e.g. for <code>concat()</code></p><p><strong><code>at(index: Integer)</code></strong> → <code>Any</code></p><p>Reads one entry out of a list — the first entry is number 1, not 0.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;a,b,c&#39;.match(&#39;[a-z]&#39;).at(1)</span></span></code></pre></div><p>Result: <code>&#39;a&#39;</code></p><p><strong><code>before(date: Date)</code></strong> → <code>Boolean</code></p><p>Checks whether the date lies before the argument.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;start&#39;).before(fieldValue(&#39;ende&#39;))</span></span></code></pre></div><p>Result: <code>true</code> when <em>start</em> lies before <em>ende</em></p><p><strong><code>beginsWith(prefix: String)</code></strong> → <code>Boolean</code></p><p>Checks whether the text begins with the given string.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;Hallo Welt&#39;.beginsWith(&#39;Hallo&#39;)</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>concat(string: String, separator: String)</code></strong> → <code>String</code></p><p>Appends another text, separated by the second argument.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;Müller&#39;.concat(&#39;Anna&#39;, &#39;, &#39;)</span></span></code></pre></div><p>Result: <code>&#39;Müller, Anna&#39;</code></p><p><strong><code>contains(substring: String)</code></strong> → <code>Boolean</code></p><p>Checks whether the text contains the given string.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;Hallo Welt&#39;.contains(&#39;Welt&#39;)</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>date()</code></strong> → <code>Date</code></p><p>The date part of a point in time, without the time of day.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;erstellt&#39;).date()</span></span></code></pre></div><p>Result: the date without time of day</p><p><strong><code>difference(date: Date)</code></strong> → <code>Duration</code></p><p>Calculates the time span between two dates.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;ende&#39;).difference(fieldValue(&#39;start&#39;))</span></span></code></pre></div><p>Result: a <code>Duration</code> — continue with <code>.asDays()</code>, for example</p><p><strong><code>displayValue()</code></strong> → <code>String</code></p><p>A readable short form of who or what created the record.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;erstelltVon&#39;).displayValue()</span></span></code></pre></div><p>Result: a readable name or an e-mail address</p><p><strong><code>divideBy(number: Number)</code></strong> → <code>Number</code></p><p>Divides the number by the argument.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;gesamtpreis&#39;).divideBy(fieldValue(&#39;menge&#39;))</span></span></code></pre></div><p>Result: the price per unit</p><p><strong><code>endsWith(postfix: String)</code></strong> → <code>Boolean</code></p><p>Checks whether the text ends with the given string.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;Hallo Welt&#39;.endsWith(&#39;Welt&#39;)</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>get(key: String)</code></strong> → <code>Any</code></p><p>Reads a value from a JSON object by its key.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;{&quot;stadt&quot;:&quot;Berlin&quot;}&#39;.parseJSON().get(&#39;stadt&#39;)</span></span></code></pre></div><p>Result: <code>&#39;Berlin&#39;</code></p><p><strong><code>greaterThan(number: Number)</code></strong> → <code>Boolean</code></p><p>Checks whether the number is greater than the argument.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>3.greaterThan(2)</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>greaterThanOrEqual(number: Number)</code></strong> → <code>Boolean</code></p><p>Checks whether the number is greater than or equal to the argument.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>3.greaterThanOrEqual(3)</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>if(if: Any, else: Any)</code></strong> → <code>Any</code></p><p>Chooses between two values depending on whether the boolean holds.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;aktiv&#39;).if(&#39;aktiv&#39;, &#39;inaktiv&#39;)</span></span></code></pre></div><p>Result: <code>&#39;aktiv&#39;</code> when the field is checked, otherwise <code>&#39;inaktiv&#39;</code></p><p><strong><code>ifUndefined(default: Any)</code></strong> → <code>Any</code></p><p>Substitutes a fallback value when the value is missing.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;spitzname&#39;).ifUndefined(&#39;unbekannt&#39;)</span></span></code></pre></div><p>Result: the field value, or <code>&#39;unbekannt&#39;</code> when the field is empty</p><p><strong><code>includesAllOf(entities: Collection)</code></strong> → <code>Boolean</code></p><p>Checks whether all of the given entries occur in the list.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;tags&#39;).includesAllOf(fieldValue(&#39;erforderlicheTags&#39;))</span></span></code></pre></div><p>Result: <code>true</code> when every wanted entry is contained</p><p><strong><code>includesAnyOf(entities: Collection)</code></strong> → <code>Boolean</code></p><p>Checks whether at least one of the given entries occurs in the list.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;tags&#39;).includesAnyOf(fieldValue(&#39;gesuchteTags&#39;))</span></span></code></pre></div><p>Result: <code>true</code> when the two lists overlap</p><p><strong><code>includesNoneOf(entities: Collection)</code></strong> → <code>Boolean</code></p><p>Checks whether none of the given entries occurs in the list.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;tags&#39;).includesNoneOf(fieldValue(&#39;ausgeschlosseneTags&#39;))</span></span></code></pre></div><p>Result: <code>true</code> when the lists do not overlap</p><p><strong><code>isEmpty()</code></strong> → <code>Boolean</code></p><p>Checks whether no value is present.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;spitzname&#39;).isEmpty()</span></span></code></pre></div><p>Result: <code>true</code> when the field is empty</p><p><strong><code>isEqualTo(other: String)</code></strong> → <code>Boolean</code></p><p>Checks whether two values are equal.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;a&#39;.isEqualTo(&#39;a&#39;)</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>isFalse()</code></strong> → <code>Boolean</code></p><p>Checks whether the value is false.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>false.isFalse()</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>isNotEmpty()</code></strong> → <code>Boolean</code></p><p>Checks whether a value is present.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;spitzname&#39;).isNotEmpty()</span></span></code></pre></div><p>Result: <code>true</code> when the field is filled</p><p><strong><code>isNotEqualTo(other: String)</code></strong> → <code>Boolean</code></p><p>Checks whether two values differ.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;a&#39;.isNotEqualTo(&#39;b&#39;)</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>isTrue()</code></strong> → <code>Boolean</code></p><p>Checks whether the value is true.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>true.isTrue()</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>isoString()</code></strong> → <code>String</code></p><p>The date as text in ISO format.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;liefertermin&#39;).isoString()</span></span></code></pre></div><p>Result: the date as text, e.g. <code>&#39;2026-08-18&#39;</code></p><p><strong><code>largeThumbnail()</code></strong> → <code>String</code></p><p>The address of a large preview of the file.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;anhang&#39;).largeThumbnail()</span></span></code></pre></div><p>Result: the address of the large preview</p><p><strong><code>lat()</code></strong> → <code>Number</code></p><p>The latitude of a geolocation — same as <code>latitude()</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;standort&#39;).lat()</span></span></code></pre></div><p>Result: the latitude as a number</p><p><strong><code>latitude()</code></strong> → <code>Number</code></p><p>The latitude of a geolocation.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;standort&#39;).latitude()</span></span></code></pre></div><p>Result: the latitude as a number</p><p><strong><code>logicalAnd(other: Boolean)</code></strong> → <code>Boolean</code></p><p>True when both values are true.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;geprüft&#39;).logicalAnd(fieldValue(&#39;freigegeben&#39;))</span></span></code></pre></div><p>Result: <code>true</code> only when both fields are checked</p><p><strong><code>logicalOr(other: Boolean)</code></strong> → <code>Boolean</code></p><p>True when at least one of the two values is true.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;dringend&#39;).logicalOr(fieldValue(&#39;überfällig&#39;))</span></span></code></pre></div><p>Result: <code>true</code> when at least one of the fields is checked</p><p><strong><code>lon()</code></strong> → <code>Number</code></p><p>The longitude of a geolocation — same as <code>longitude()</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;standort&#39;).lon()</span></span></code></pre></div><p>Result: the longitude as a number</p><p><strong><code>longitude()</code></strong> → <code>Number</code></p><p>The longitude of a geolocation.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;standort&#39;).longitude()</span></span></code></pre></div><p>Result: the longitude as a number</p><p><strong><code>match(pattern: String)</code></strong> → <code>Collection</code></p><p>Collects all matches of a search pattern as a list.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;a,b,c&#39;.match(&#39;[a-z]&#39;)</span></span></code></pre></div><p>Result: a list with 3 matches: <code>&#39;a&#39;</code>, <code>&#39;b&#39;</code>, <code>&#39;c&#39;</code></p><p><strong><code>minus(number: Number)</code></strong> → <code>Number</code></p><p>Subtracts the argument from the number.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;brutto&#39;).minus(fieldValue(&#39;rabatt&#39;))</span></span></code></pre></div><p>Result: the difference of both fields</p><p><strong><code>multiplyBy(number: Number)</code></strong> → <code>Number</code></p><p>Multiplies the number by the argument.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;preis&#39;).multiplyBy(1.19)</span></span></code></pre></div><p>Result: the price including a 19 % surcharge</p><p><strong><code>parseJSON()</code></strong> → <code>KeyValueObject</code></p><p>Reads text as JSON and makes its fields accessible with <code>get()</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;{&quot;stadt&quot;:&quot;Berlin&quot;}&#39;.parseJSON()</span></span></code></pre></div><p>Result: a JSON object — continue with <code>.get(&#39;stadt&#39;)</code></p><p><strong><code>plus(number: Number)</code></strong> → <code>Number</code></p><p>Adds two numbers.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;grundpreis&#39;).plus(fieldValue(&#39;versand&#39;))</span></span></code></pre></div><p>Result: the sum of both fields</p><p><strong><code>round(number: Number)</code></strong> → <code>Number</code></p><p>Rounds half up to the given number of decimal places.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;preis&#39;).round(2)</span></span></code></pre></div><p>Result: the price with 2 decimal places</p><p><strong><code>roundDown(number: Number)</code></strong> → <code>Number</code></p><p>Rounds to the given number of decimal places — always downwards.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;preis&#39;).roundDown(0)</span></span></code></pre></div><p>Result: the next smaller whole number</p><p><strong><code>roundUp(number: Number)</code></strong> → <code>Number</code></p><p>Rounds to the given number of decimal places — always upwards.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;preis&#39;).roundUp(0)</span></span></code></pre></div><p>Result: the next larger whole number</p><p><strong><code>second()</code></strong> → <code>Duration</code></p><p>Builds a time span in seconds from a number.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;wartezeit&#39;).second()</span></span></code></pre></div><p>Result: a time span, e.g. 5 seconds</p><p><strong><code>seconds()</code></strong> → <code>Duration</code></p><p>Builds a time span in seconds from a number — same as <code>second()</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;wartezeit&#39;).seconds()</span></span></code></pre></div><p>Result: a time span, e.g. 5 seconds</p><p><strong><code>sha256()</code></strong> → <code>String</code></p><p>Computes the SHA-256 hash of the text — 64 lowercase hexadecimal characters, calculated over the text as UTF-8. The same text always produces the same hash; it cannot be reversed.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;Hallo Welt&#39;.sha256()</span></span></code></pre></div><p>Result: <code>&#39;2d2da19605a34e037dbe82173f98a992a530a5fdd53dad882f570d4ba204ef30&#39;</code></p><p><strong><code>size()</code></strong> → <code>Integer</code></p><p>Counts the entries of a list.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;a,b,c&#39;.match(&#39;[a-z]&#39;).size()</span></span></code></pre></div><p>Result: <code>3</code></p><p><strong><code>smallThumbnail()</code></strong> → <code>String</code></p><p>The address of a small preview of the file.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;anhang&#39;).smallThumbnail()</span></span></code></pre></div><p>Result: the address of the small preview</p><p><strong><code>smallerThan(number: Number)</code></strong> → <code>Boolean</code></p><p>Checks whether the number is smaller than the argument.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>2.smallerThan(3)</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>smallerThanOrEqual(number: Number)</code></strong> → <code>Boolean</code></p><p>Checks whether the number is smaller than or equal to the argument.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>2.smallerThanOrEqual(2)</span></span></code></pre></div><p>Result: <code>true</code></p><p><strong><code>substring(from: Number, to: Number)</code></strong> → <code>String</code></p><p>Cuts a section out of the text — the first character is number 1, as with <code>at()</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;Hallo Welt&#39;.substring(1, 5)</span></span></code></pre></div><p>Result: <code>&#39;Hallo&#39;</code></p><p><strong><code>subtract(amount: Number, unit: String)</code></strong> → <code>DateAndTime</code></p><p>Subtracts an amount in the given unit. As with <code>add()</code>, <code>&#39;day&#39;</code>, <code>&#39;week&#39;</code>, <code>&#39;year&#39;</code>, <code>&#39;hour&#39;</code> and <code>&#39;minute&#39;</code> work, <code>&#39;month&#39;</code>/<code>&#39;months&#39;</code> do not — for days use <code>subtractDays()</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;start&#39;).subtract(1, &#39;week&#39;)</span></span></code></pre></div><p>Result: the date one week earlier</p><p><strong><code>subtractDays(numberofDays: Number)</code></strong> → <code>Date</code></p><p>Subtracts days from a date.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;liefertermin&#39;).subtractDays(7)</span></span></code></pre></div><p>Result: the date one week earlier</p><p><strong><code>toTimezoneString(timezone: String)</code></strong> → <code>String</code></p><p>Formats date and time for another time zone.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;zeitstempel&#39;).toTimezoneString(&#39;Europe/Berlin&#39;)</span></span></code></pre></div><p>Result: the timestamp in the given time zone</p><p><strong><code>url()</code></strong> → <code>String</code></p><p>The address from which the file can be downloaded.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;anhang&#39;).url()</span></span></code></pre></div><p>Result: the download address as text</p><p><strong><code>add(element: Any)</code></strong> → <code>Collection</code> — on <code>Collection</code></p><p>Appends an entry to the end of the list. Not to be confused with <code>add()</code> on a date, which adds an amount of time.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&#39;a,b,c&#39;.match(&#39;[a-z]&#39;).add(&#39;d&#39;)</span></span></code></pre></div><p>Result: the list <code>a</code>, <code>b</code>, <code>c</code>, <code>d</code></p><p><strong><code>minus(date: Date)</code></strong> → <code>Duration</code> — on <code>Date</code></p><p>Subtracts a date from a date and returns the time span in between — not to be confused with <code>minus()</code> on numbers. Continue with <code>asDays()</code> or <code>asHours()</code> for days or hours.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;ende&#39;).minus(fieldValue(&#39;start&#39;))</span></span></code></pre></div><p>Result: the time span from <em>start</em> to <em>ende</em> as a <code>Duration</code></p><p><strong><code>plus(duration: Duration)</code></strong> → <code>Duration</code> — on <code>Date</code></p><p>Adds a time span to a date. The result is a point in time with a time of day, even though the signature says <code>Duration</code>. For whole days, <code>addDays()</code> is the simpler way.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;start&#39;).plus(fieldValue(&#39;wartezeit&#39;).seconds())</span></span></code></pre></div><p>Result: the point in time <em>wartezeit</em> seconds after <em>start</em></p><p><strong><code>minus(dateAndTime: DateAndTime)</code></strong> → <code>Duration</code> — on <code>DateAndTime</code></p><p>Subtracts a point in time from a point in time and returns the time span in between — not to be confused with <code>minus()</code> on numbers. Continue with <code>asHours()</code> or <code>asMinutes()</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;ende&#39;).minus(fieldValue(&#39;start&#39;))</span></span></code></pre></div><p>Result: the time span from <em>start</em> to <em>ende</em> as a <code>Duration</code></p><p><strong><code>plus(dateAndTime: DateAndTime)</code></strong> → <code>Duration</code> — on <code>DateAndTime</code></p><p>Adds a time span to a point in time. The result is again a point in time, even though the signature names <code>Duration</code> as the result and a <code>DateAndTime</code> as the argument.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;start&#39;).plus(fieldValue(&#39;wartezeit&#39;).seconds())</span></span></code></pre></div><p>Result: the point in time <em>wartezeit</em> seconds after <em>start</em></p><p><strong><code>add(element: String)</code></strong> → <code>EntityCollection</code> — on <code>EntityCollection</code></p><p>Links one more record by its id — the same as <code>addEntity()</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fieldValue(&#39;verknuepft&#39;).add(fieldValue(&#39;neuerEintrag&#39;))</span></span></code></pre></div><p>Result: the list with the additional record</p><h2 id="next" tabindex="-1">Next <a class="header-anchor" href="#next" aria-label="Permalink to &quot;Next&quot;">​</a></h2><ul><li><a href="/en/apptivescript/schreibweise">Syntax</a> — dots, parentheses, quotation marks</li><li><a href="/en/apptivescript/werte-referenzieren">Referencing values</a> — what an expression starts with</li><li><a href="/en/grids/berechnete-felder">Calculated fields</a> — creating formula columns</li><li><a href="/en/seiten/sichtbarkeit">Conditional visibility</a> — showing blocks based on input</li><li><a href="/en/referenz/filterbedingungen">Filter conditions</a> — the same comparisons via the interface</li><li><a href="/en/referenz/glossar">Glossary</a></li></ul>',363)])])}const g=t(s,[["render",n]]);export{u as __pageData,g as default};
